name: Generar APK Fabrica

on:
  push:
    branches: [ "main" ]
  workflow_dispatch: # Permite forzar la compilación con un botón

jobs:
  build:
  
  - name: 🔑 Dar permisos al ejecutable de Gradle
        run: chmod +x android/gradlew
      - name: 🔨 Compilar APK (Release)
        run: flutter build apk --release

    steps:
      - name: 📥 Descargar Código
        uses: actions/checkout@v4

      - name: ☕ Instalar Java 17
        uses: actions/setup-java@v4
        with:
          distribution: 'zulu'
          java-version: '17'

      - name: 🐦 Instalar Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: 'stable'
          flutter-version: '3.19.0' # Versión estable recomendada

      - name: 📦 Descargar Librerías
        run: flutter pub get

      # ESTE PASO ES CRÍTICO: Genera el código de la base de datos
      - name: 🏗️ Generar Archivos (Drift/Riverpod)
        run: dart run build_runner build --delete-conflicting-outputs

      - name: 🔨 Compilar APK (Release)
        run: flutter build apk --release

      - name: 📤 Subir APK a GitHub
        uses: actions/upload-artifact@v4
        with:
          name: app-fabrica-release
          path: build/app/outputs/flutter-apk/app-release.apk
