name: gestion_fabrica
description: Aplicación de gestión de fábrica con roles, contabilidad y exportación PDF

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  
  # Estado
  flutter_riverpod: ^2.4.9
  riverpod_annotation: ^2.3.3
  
  # Base de datos
  drift: ^2.14.0
  sqlite3_flutter_libs: ^0.5.18
  path_provider: ^2.1.1
  path: ^1.8.3
  
  # PDF
  pdf: ^3.10.7
  printing: ^5.11.1
  share_plus: ^7.2.1
  
  # UI
  google_fonts: ^6.1.0
  flutter_slidable: ^3.0.1
  fl_chart: ^0.66.0
  intl: ^0.18.1
  shimmer: ^3.0.0
  
  # Utilidades
  freezed_annotation: ^2.4.1
  json_annotation: ^4.8.1
  uuid: ^4.2.1
  collection: ^1.18.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1
  build_runner: ^2.4.7
  drift_dev: ^2.14.0
  riverpod_generator: ^2.3.9
  freezed: ^2.4.5
  json_serializable: ^6.7.1
  custom_lint: ^0.5.7
  riverpod_lint: ^2.3.7

flutter:
  uses-material-design: true
  
  assets:
    - assets/images/
    - assets/fonts/
